@using SwdPageRecorder.UI;
@using SwdPageRecorder.UI.CodeGeneration


@{

    WebElementDefinition[] definitions = Model.WebElementDefinitions;
    ExternalGenerator run = Model.ExternalGenerator;

	string jsonFile = "WebElementDefinitions.json";
	string commandLine = "cscript //Nologo sample_ParserWebElements.js";

	run.SaveToJSonFile(definitions, jsonFile);


}

@Raw(@run.RunExternalProcess(commandLine))
